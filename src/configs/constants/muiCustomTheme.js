import { createTheme } from "@mui/material";

export const THEME = createTheme({
  palette: {
    blueTheme: {
      main: "#3a78ff",
      dark: "#2c59bc",
      contrastText: "rgba(255, 255, 255, 1)",
    },
  },
});
